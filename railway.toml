[build]
builder = "docker"
dockerfilePath = "Dockerfile"

[deploy]
service = "chandamama-rag"
startCommand = "streamlit run app.py --server.port=8501 --server.address=0.0.0.0"
healthcheckPath = "/"
restartPolicyType = "on-failure"

[[mounts]]
sourceVolume = "qdrant_data"
mountPath = "/data"
